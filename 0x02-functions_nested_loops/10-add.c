/**
 * add- a function that adds two integers and returns the result
 * Return: Always 0.
 * @a:first value to be computed
 * @b:second value to be computed
 */
int add(int a, int b)
{
return (a + b);
}
